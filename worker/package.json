{
  "name": "bratan-muzonchik-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for SoundCloud — CORS + client_id auto-refresh for bratan-muzonchik music player",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
